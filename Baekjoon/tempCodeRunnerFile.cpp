ile (!q.empty()) {
    //     auto [x, y] = q.front();
    //     q.pop();

    //     if (visited[x][y] & 1) continue;
    //     visited[x][y] |= 1;
    //     if (++cnt == 7) return true;

    //     for (const auto [dx, dy]: dir) {
    //         int nx = x + dx, ny = y + dy;

    //         if (nx < 0 || ny < 0 || nx >= 5 || ny >= 5) continue;
    //         if (visited[nx][ny] == 2) q.push({dx, dy});
    //     }
    // }